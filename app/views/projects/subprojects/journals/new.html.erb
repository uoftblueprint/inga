<div class="mx-8 my-10">
  <h2 class="text-4xl font-bold mb-5"><%= t(".title") %></h2>
  <%= turbo_frame_tag "journal_new_form" do %>
    <%= render Journals::FormComponent.new(project: @project, subproject: @subproject, journal: @journal) %>
  <% end %>
</div>
