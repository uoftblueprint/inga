<div class="mx-8 my-10">
  <div class="flex flex-col gap-4">
    <div class="flex flex-row justify-between items-center">
      <h1 class="text-4xl">
        Projects
      </h1>
      <%= link_to new_project_path, data: { turbo_stream: true }, class: "text-base-100 bg-primary flex flex-row items-center gap-2 text-xs py-2.5 px-3 rounded" do %>
        <%= render IconComponent.new(icon: "add", size: :sm) %> Create Project
      <% end %>
    </div>
    <%= render Home::ProjectsCardComponent.new(projects: @projects) %>
  </div>
</div>
