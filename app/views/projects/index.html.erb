<div class="mx-8 my-10">
  <div class="flex flex-col gap-4">
    <div class="flex flex-row justify-between items-center">
      <h1 class="text-4xl">
        Projects
      </h1>
      <button
        type="button"
        class="text-white bg-primary flex flex-row items-center gap-2 text-xs py-2.5 px-3 rounded hover:cursor-pointer">
        <%= render IconComponent.new(icon: "add", size: :sm) %> Create Project
      </button>
    </div>
    <%= render Home::ProjectsCardComponent.new(projects: @projects) %>
  </div>
</div>
