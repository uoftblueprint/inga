<%= turbo_stream.replace "modal" do %>
  <%= render ModalComponent.new(title: t(".title")) do %>
    <%= render Subprojects::FormComponent.new(project: @project, subproject: @subproject) %>
  <% end %>
<% end %>
