<div class = "max-w-3x1 mx-auto my-8 p-6 bg-base-100 border border-base-200">
    <div class = "space-y-2"> 
    <p><strong>Description:</strong><%= @subproject.description %></p>
    <p><strong>Address:</strong><%= @subproject.address %></p>
    <p><strong>Region:</strong><%= @subproject.region&.name || "No region" %></p>
    <p><strong>Project</strong><%= @project.name %></p>
    <p class = "text-muted"> Created: <%= l(@subproject.created_at, format: :short) if @subproject.created_at %></p>    
    </div>

    <div class = "mt-4"> 
    <%= link_to "Back to Project", root_path, class: "btn btn-outline btn-sm" %>
    </div>

  </div>