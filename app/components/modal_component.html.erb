<button class="btn" onclick="<%= @id %>.showModal()"><%= @button_text %></button>
<dialog id="<%= @id %>" class="modal">
  <div class="modal-box w-full max-w-xl">
    <%= modal_content %>
  </div>
  <form method="dialog" class="modal-backdrop">
    <button>close</button>
  </form>
</dialog>
